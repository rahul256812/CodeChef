#include <stdio.h>

int main() {
    // Defining the two numbers
    int num1 = 44;
    int num2 = 88;
    
    // Computing the sum
    int sum = num1 + num2;
    
    // Displaying the result
    printf("The sum of %d and %d is %d\n", num1, num2, sum);
    
    return 0;
}
